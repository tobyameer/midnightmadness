[build]
  base = "midnight-madness-landing/Client"
  publish = "dist"
  command = "npm ci --include=dev && npm run build"

[build.environment]
  NODE_VERSION = "20"
  NPM_CONFIG_PRODUCTION = "false"  # ensure devDependencies (vite) are installed